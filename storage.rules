rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Resource files — publicly readable, only authenticated (admin) can write/delete
    match /resources/{allPaths=**} {
      allow read:          if true;
      allow write, delete: if request.auth != null;
    }
  }
}
